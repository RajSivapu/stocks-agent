{
  "name": "stocks-agent",
  "private": true,
  "workspaces": [
    "packages/dashboard-contracts",
    "apps/web"
  ],
  "scripts": {
    "test:contracts": "npm test --workspace @stocks-agent/dashboard-contracts",
    "test:web": "npm test --workspace @stocks-agent/web"
  },
  "engines": {
    "node": ">=24"
  }
}
